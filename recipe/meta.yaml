{% set name = "ndtiff" %}
{% set version = "1.6.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/ndtiff-{{ version }}.tar.gz
  sha256: 86e39908ff965e9ad2496ed0170fe8327a020e543b34cf3b3777cf88bc63f08c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - dask-core >=2022.2.0
    - numpy
    - python >=3.6

test:
  imports:
    - ndtiff
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://github.com/micro-manager/NDTiffStorage
  summary: Python libraries for NDTiff datasets
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - goanpeca
    - henrypinkard
